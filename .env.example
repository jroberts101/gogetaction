# General
NODE_ENV=development

# Backend API
BACKEND_PORT=5000
MONGO_URI=mongodb://mongo:27017/gogetaction

# Frontend (Vite)
VITE_BACKEND_URL=https://localhost/api

# Keycloak
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=admin123
KEYCLOAK_REALM=gogetaction
KEYCLOAK_CLIENT_ID=frontend
KEYCLOAK_BACKEND_CLIENT_ID=backend-api

# Postgres (for Keycloak)
POSTGRES_USER=keycloak
POSTGRES_PASSWORD=keycloak

# Email Configuration
# For development (Mailpit)
EMAIL_FROM_ADDRESS=no-reply@gogetaction.example.com
SMTP_HOST=mailpit
SMTP_PORT=1025
SMTP_SECURE=false
# No auth needed for Mailpit by default, but supported with MP_SMTP_AUTH_ACCEPT_ANY=1
# SMTP_USER=user
# SMTP_PASS=pass

# For production (SendGrid)
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_SECURE=true
# SMTP_USER=apikey
# SMTP_PASS=YOUR_SENDGRID_API_KEY

# Example Third-Party API Keys (Add as needed)
# STRIPE_SECRET_KEY=sk_test_YOUR_STRIPE_SECRET_KEY
# STRIPE_PUBLISHABLE_KEY=pk_test_YOUR_STRIPE_PUBLISHABLE_KEY
# VITE_STRIPE_PUBLISHABLE_KEY=pk_test_YOUR_STRIPE_PUBLISHABLE_KEY