export interface CampaignFormData {
  title: string;
  description: string;
  category: string;
  goal: number;
}

export interface LetterTemplate {
  id: string;
  title: string;
  content: string;
}
